层序遍历(队列)
   反转树, 求树深

递归
    前、中、后
求和可以，做减法，在子树里找

完全二叉树性质：  剪枝
若左到底  =  右到底， 则为满二叉树

判断平衡二叉树，递归，底层报错就网上报


属性篇（深度、高度、平衡）。

路径篇（找路径、算总和、回溯）

路径总和：           
进房间时：把数字写上去 
进去之后判断有没有左右，没有就看==0  有就递归
出房间时：把数字擦掉

二叉搜索树：  验证——中序遍历&&严格递增
共同祖先：
空直接返回root
不空的情况下，都小，root=root->left;
            都大，root=root->right;
            否则，直接返回
            
插入用递归，找到直接返回return new Tree Node（val）；
删除用递归，三个情况，只有左，只有右，左右都有就要把左放到右的最左

Nums2Tree ：二分查找，递归
